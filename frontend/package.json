{
  "name": "tailwind-monorepo",
  "version": "1.0.0",
  "description": "Monorepo with Vite React, React Native, Tailwind CSS 4, and Supabase",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:native": "pnpm --filter native start",
    "build:web": "pnpm --filter web build",
    "build:native": "pnpm --filter native build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tailwindcss": "^4.0.0-alpha.25",
    "@tailwindcss/postcss": "^4.0.0-alpha.25",
    "postcss": "^8.4.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}